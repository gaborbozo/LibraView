services:
  keycloak:
    environment:
      KC_DB_USERNAME:
      KC_DB_PASSWORD:

      KEYCLOAK_ADMIN:
      KEYCLOAK_ADMIN_PASSWORD:
    ports:
      - :8080

  keycloak_db:
    environment:
      POSTGRES_USER:
      POSTGRES_PASSWORD:
    ports:
      - :5432
  libra_view_master:
    environment:
      POSTGRES_USER:
      POSTGRES_PASSWORD:
    ports:
      - :5432
  libra_view_cine_registry:
    environment:
      POSTGRES_USER:
      POSTGRES_PASSWORD:
    ports:
      - :5432
