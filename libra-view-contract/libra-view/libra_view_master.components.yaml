components:
  schemas:
    LoginRequest:
      type: object
      required:
        - username
        - password
      properties:
        username:
          type: string
        password:
          type: string
    AuthNResponse:
      type: object
      properties:
        token:
          type: string
        expiration:
          type: number
          format: int64